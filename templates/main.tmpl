{{define "main"}}
{{template "header" .}}
<div class="content">

	<div class="container">
		<div class="col-md-12 mb-4">
			<h2>RWTH Link Shortener - {{.user}}</h2>
			<div>{{.URLCount}} Links have been shortened</div>
		</div>
	</div>
{{template "footer" .}}
{{end}}
